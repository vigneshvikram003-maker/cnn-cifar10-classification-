# cnn-cifar10-classification-
This project implements a Convolutional Neural Network (CNN) using TensorFlow and Keras to classify CIFAR-10 images into 10 categories. The model preprocesses images, trains on the dataset, and predicts the class of a test image, displaying the final predicted label.
